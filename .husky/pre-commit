#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 1. lint-staged (Lint Fix + Stage)
# lint-staged는 내부적으로 biome check --write 후 변경된 파일을 다시 git add 해줍니다.
# 그 후 biome check를 한 번 더 실행하여 잔여 에러를 확인하도록 package.json에 설정했습니다.
pnpm exec lint-staged || exit 1

# 2. Type Check
# 전체 프로젝트의 타입 안정성을 검사합니다.
pnpm type-check || exit 1
